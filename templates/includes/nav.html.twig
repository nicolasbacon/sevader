<div class="navbar-nav">
    <a class="nav-item nav-link active" href="{{ path('main_home') }}">ACCUEIL</a>
    {% if is_granted ("ROLE_ADMIN") %}
    <a class="nav-item nav-link" href="#">VILLES</a>
    <a class="nav-item nav-link" href="#">SITES</a>
    <a class="nav-item nav-link" href="{{ path('participant_new') }}">AJOUTER UN PARTICIPANT</a>
    <a class="nav-item nav-link" href="{{ path('participant_upload') }}">IMPORT PARTICIPANTS CSV</a>
    {% endif %}

    {% if is_granted ("ROLE_USER") or is_granted ("ROLE_ADMIN") %}
    <a class="nav-item nav-link" href="{{ url('participant_editer_profil') }}">MON PROFIL</a>
    {% endif %}

    {% if not is_granted ("ROLE_USER") or not is_granted ("ROLE_ADMIN") %}
    <a class="nav-item nav-link" href="{{ path('app_login') }}">SE CONNECTER</a>
    {% endif %}

    {% if is_granted ("ROLE_USER") or is_granted ("ROLE_ADMIN") %}
    <a class="nav-item nav-link" href="{{ path('app_logout') }}">SE DECONNECTER</a>
    {% endif %}
</div>