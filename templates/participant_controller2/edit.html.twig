{% extends 'base.html.twig' %}

{% block title %}Edit Participant{% endblock %}

{% block body %}
    <h1>Edit Participant</h1>

    {{ include('participant_controller2/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('app_participant_controller2_index') }}">back to list</a>

    {{ include('participant_controller2/_delete_form.html.twig') }}
{% endblock %}
